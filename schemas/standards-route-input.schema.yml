$schema: https://json-schema.org/draft/2020-12/schema
$id: https://zipzap.local/schemas/standards-route-input.schema.yml
title: Standards route input
type: object
additionalProperties: false
required: [schema_version, project]
properties:
  schema_version: { const: 1 }
  project:
    type: object
    additionalProperties: false
    required: [locator]
    properties:
      locator: { type: string, minLength: 1 }
      standards:
        description: Existing project-relative Markdown files or directories; overrides conventional source discovery without moving files.
        type: array
        minItems: 1
        uniqueItems: true
        items: { type: string, minLength: 1 }
      documents:
        description: Existing project-relative Markdown entry or index files for one-hop reference discovery; never promoted to standards.
        type: array
        uniqueItems: true
        items: { type: string, minLength: 1 }
  context:
    type: object
    additionalProperties: false
    properties:
      action: { type: string, minLength: 1 }
      risk: { enum: [low, medium, high] }
      domains:
        type: array
        items: { type: string, pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$" }
        uniqueItems: true
      artifacts:
        type: array
        items: { type: string, pattern: "^[a-z0-9]+(?:-[a-z0-9]+)*$" }
        uniqueItems: true
      paths:
        type: array
        items: { type: string, minLength: 1 }
        uniqueItems: true
